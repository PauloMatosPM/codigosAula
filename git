# codigosAula
vai na pasta inicializa o git
vai na sua conta do git e cria o repositorio 
        --
adiciona os arquivos na area de preparação do git 
        git add --all
faça as configurações 
        git config.user.email "seu email"
        git config.user.email "seu username"
de commit 
        git commit -m "alguma mensagem"
adicione o repositorio 
        git remote add origin seu_link
depois de o push do repositorio local para o repositorio remoto
        git push -u origin master                                
